mixin socials(blockName, className, items)
    ul(class=`${blockName}__list ${className} ${className}__list`)
      each item in items
          li(class=`${className}__item`)
              a(class=`${className}__link ${item.name}` href=item.link alt=item.name)
